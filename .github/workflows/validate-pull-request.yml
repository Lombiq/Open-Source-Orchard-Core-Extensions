name: "Maintenance"
on:
  push:
  pull_request_target:
    types: [synchronize]

jobs:
  call-check-merge-conflict:
    uses: Lombiq/GitHub-Actions/.github/workflows/check-merge-conflict.yml@issue/OSOE-144